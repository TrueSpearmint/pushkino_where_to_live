// Функция для обновления значения коэффициента школ
let schools_coefficient = 1;
function updateSchoolsCoefficient(value) {
  schools_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента детских садов
let kindergartens_coefficient = 1;
function updateKindergartensCoefficient(value) {
  kindergartens_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента поликлиник
let clinics_coefficient = 1;
function updateClinicsCoefficient(value) {
  clinics_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента фитнес-центров
let fitness_coefficient = 1;
function updateFitnessCoefficient(value) {
  fitness_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента прогулок у воды
let waterwalk_coefficient = 1;
function updateWaterwalkCoefficient(value) {
  waterwalk_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента предприятий торговли и услуг
let commerce_coefficient = 1;
function updateCommerceCoefficient(value) {
  commerce_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента состояния жилищного фонда
let buildings_quality_coefficient = 1;
function updateBuildingsQualityCoefficient(value) {
  buildings_quality_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента близости к Ростокино на авто
let rostokino_car_coefficient = 1;
function updateRostokinoCarCoefficient(value) {
  rostokino_car_coefficient = parseInt(value);
}

// Функция для обновления значения коэффициента близости к Ростокино на авто
let rostokino_bus_coefficient = 1;
function updateRostokinoBusCoefficient(value) {
  rostokino_bus_coefficient = parseInt(value);
}
